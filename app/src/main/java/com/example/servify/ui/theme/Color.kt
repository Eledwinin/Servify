package com.example.servify.ui.theme


import androidx.compose.ui.graphics.Color

//Colores Principales de Marca
val ServifyGreenPrimary = Color(0xFF238668)       // Botón principal, logo, banner superior
val ServifyGreenDark = Color(0xFF1B6A52)          // Estados pressed / hover
val ServifyGreenLight = Color(0xFFE8F5F1)         // fondo de badges, "Ver detalles", chips activos
val ServifyTealAvatar = Color(0xFFD1F2E8)         // Círculos de avatar (JP, AM)
val ServifyTealAvatarText = Color(0xFF1E7B62)     // textos iniciales avatar

// Superficies y fondos
val ServifyBackground = Color(0xFFF8FAFC)         // fondo general de la app (gris ultra claro)
val ServifySurface = Color(0xFFFFFFFF)            // fondo de cards y modales
val ServifyInputBackground = Color(0xFFF1F5F9)    // fondo de barras de búsqueda y chats
val ServifyBorder = Color(0xFFE2E8F0)             // Bordes de tarjetas y separadores

// --- Tipografía y textoss ---
val ServifyTextTitle = Color(0xFF0F172A)          // Títulos principales en negrita
val ServifyTextBody = Color(0xFF334155)           // textos estándar
val ServifyTextMuted = Color(0xFF64748B)          // Subtítulos, fechas
val ServifyTextPlaceholder = Color(0xFF94A3B8)    // Placeholder en inputs

// --- Estados y Tags (Badges) ---
val ServifyPendingBg = Color(0xFFFEF3C7)          // fondo tag "PENDIENTE"
val ServifyPendingText = Color(0xFFD97706)        // textos tag "PENDIENTE"
val ServifyStarYellow = Color(0xFFFBBF24)         // Estrellas de calificación en reseñas
val ServifyOnlineGreen = Color(0xFF10B981)        // textos "En línea"

//Acciones Críticas
val ServifyDanger = Color(0xFFEF4444)             // textos/ícono "Cerrar Sesión"
val ServifyDangerBg = Color(0xFFFEF2F2)           // fondo botón "Cerrar Sesión"

// Elementos Flotantes / Botones Secundarios
val ServifyFabDark = Color(0xFF1E293B)            // Botón flotante + / Ver en Mapa